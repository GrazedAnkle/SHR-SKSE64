{
  // Pylance reads neither the published binding's location nor pytest's import root, so both are named
  // again here. See tools/README.md.
  "python.analysis.extraPaths": [
    "build/module",
    "tools"
  ]
}
