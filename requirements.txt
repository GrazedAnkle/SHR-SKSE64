# Python dependencies for reference analysis and annotation tools (tools/).
numpy>=1.24
scipy>=1.10        # signal.fftconvolve for fast whole-file filtering in auto_annotate.py
soundfile>=0.12    # WAV I/O (shrlib.load)
matplotlib>=3.7    # used only for the breath modulation-adjudication view (auto_annotate --breath --view)
                   # imported lazily
pybind11>=3.0      # build-time only: offline core binding (BUILD_PYBIND); provides the CMake config
                   # and headers to build shr_pybind against the venv's Python
pybind11-stubgen>=2.5  # build-time only: generates build/pybind/shr_pybind.pyi for Pylance/Pyright
pytest>=8.0        # golden regression suite over the compiled offline binding
